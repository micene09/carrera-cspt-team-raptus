name: publish

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'reason'
        required: false
        default: 'manual run'
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - run: corepack enable
    - name: use node 22
      uses: actions/setup-node@v7
      with:
        node-version: 22.x
        registry-url: https://registry.npmjs.org
        cache: npm
    - name: setup
      uses: actions/configure-pages@v6
    - run: npm ci
    - run: npm run build
    - name: upload github pages artifact
      uses: actions/upload-pages-artifact@v5
      with:
        path: dist

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: "pages"
      cancel-in-progress: true
    environment:
      name: github-pages
      url: https://github.com/micene09/carrera-cspt-team-raptus
    steps:
      - name: deploy
        id: deployment
        uses: actions/deploy-pages@v4
